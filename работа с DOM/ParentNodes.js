/** Кроме деления по типам, узлы делятся на дочерние и родительские. Соответственно, для обращения к элементам, вложенным в DOM-узел, существует два типа навигационных ссылок:

 Для всех узлов:

 parentNode

 Только для узлов-элементов:

 parentElement */