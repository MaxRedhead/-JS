/**tem — очередной элемент массива,

    index — его индекс,

    array — сам массив.

arr.forEach(function(item, index, array) {
    // возвращается новое значение вместо элемента
});*/

let arr = [1,2,3]

arr.forEach(function(item, index, array) {

    console.log(item)

});